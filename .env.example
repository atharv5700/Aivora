# Aivora Environment Variables Example
# Copy this file to .env.local and fill in your API keys

# Google Gemini API Key (optional - can be configured in UI)
# Get your key from: https://ai.google.dev/
VITE_GEMINI_API_KEY=

# Note: All API keys can be configured via the Settings UI in the app.
# API keys are stored in browser localStorage and never sent to any server
# except the AI provider's API endpoint directly from your browser.
